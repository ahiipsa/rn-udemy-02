import React from 'react';
import {StyleSheet, View} from 'react-native';

type Props = {};
export const BookedScreen: React.FC<Props> = () => {
  return (
    <View>

    </View>
  );
};

const styles = StyleSheet.create({});
